apiVersion: v1
kind: Service

metadata:
  name: nvidia-dcgm-exporter
  namespace: hpc-platform-dev

spec:
  selector:
    app: nvidia-dcgm-exporter

  ports:
    - name: metrics
      port: 9400
      targetPort: 9400
