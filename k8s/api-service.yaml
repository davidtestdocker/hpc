apiVersion: v1
kind: Service

metadata:
  name: api-service
  namespace: hpc-platform

spec:
  type: NodePort
  selector:
    app: api

  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080  


