# Main demo values are independent of the historical dev GitOps image tag.
image:
  tag: automatic-worker-20260922-v1
worker:
  enabled: true
service:
  type: ClusterIP
  port: 8000
  targetPort: 8000
ingress:
  enabled: false
