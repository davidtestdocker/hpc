# Week1 — Linux：先理解服務如何執行

現行教材版本：2026-09-22。先讀本頁，再按 Day 順序閱讀；不必先讀懂整個專案。

## 先備與學習方式

從這裡開始；只需要能開終端機和文字檔。 每一課先解釋概念，再直接讀現行補充、已保存結果與原本完整教學。原本完整教材與輸出已放回每一課下半部；前面是現行補充與已有結果，無須重新操作。

## 基礎解說

程式是磁碟上的指令；程序是執行中的實例，有 PID、位址空間與資源。執行緒是程序內可被排程的執行單位，多執行緒不等於多台機器。

CPU 排程把可執行的執行緒分配到邏輯 CPU；等待磁碟或網路的工作通常不能靠增加 CPU 解決。CPU 百分比是時間比例，load average 是另一種負載指標，不能直接視為百分比。

記憶體要分程序 RSS、cache 與可用量；磁碟要分容量、吞吐、延遲。定位瓶頸先問「誰在等待什麼」，再挑工具，不是看到單一數字高就下結論。

## 目前環境與實測邊界

### 先認得這幾個詞

| 名詞 | 白話意思 | 本週不要混淆的事 |
|---|---|---|
| Program | 尚未執行的程式檔案 | 檔案存在不表示有程序在跑 |
| Process／PID | 執行中的實例／它的識別號 | 同一程式可有多個 PID |
| Thread | 程序內可被排程的執行單位 | 一個程序可能同時用多個核心 |
| CPU core／logical CPU | 硬體計算資源／系統可排程單位 | 不等於機器數量；SMT 也不代表效能線性加倍 |
| Waiting | 等待 I/O、鎖或其他事件 | 不一定是當機 |
| Context switch | 把 CPU 的執行對象換成另一個 | 切換多不必然代表需要優化 |

例如：API 程序等待 DB 回應時，CPU 可以執行其他可執行的執行緒。若瓶頸是 DB 等待，單純讓 API 取得更多 CPU 不一定改善延遲。這就是先確認等待原因、再調資源的原因。

本週在自己的 Linux 學習環境做唯讀觀察，不聲稱主叢集當下健康。

[本週實作／證據入口](<../evidence/README.md>)。本週的原始碼、manifest 與保存的成功／失敗各有不同證明力，不能全部當成今天又測過一次。

## 每日閱讀順序

- [Day1：程序與 PID](<day1-linux-process.md>)
- [Day2：CPU 排程](<day2-cpu-scheduler.md>)
- [Day3：Context switch](<day3-context-switch.md>)
- [Day4：CPU utilization](<day4-cpu-utilization.md>)
- [Day5：Memory](<day5-memory.md>)
- [Day6：Disk I/O](<day6-disk-io.md>)
- [Day7：系統效能分析流程](<day7-performance-analysis.md>)

## 直接讀結果，不要求重跑

每課的「已有結果與解讀」列出可用的已保存證據；「原始完整教材與當時輸出」保留整篇舊文。命令當作理解當時做法的材料，不需要你再開 VM 或在本機測試。沒有保存的實測結果會明說，示例不當作真實驗收。

讀到不熟的地方先回本週概念，再看輸出與解讀；不用自己重建環境找答案。
